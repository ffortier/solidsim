describe('boardController', function() {
    'use strict';

    beforeEach(module('appComponents'));

    it('should do something', inject(function(geometryFactory) {

        expect(geometryFactory).toBeDefined();

    }));

});